# Claude Handoff

Claude powers Albert through `netlify/functions/generate.js`. Preserve server-side key handling. Return structured JSON for intake, never invent equipment identifiers, and flag assumptions in proposal output.
